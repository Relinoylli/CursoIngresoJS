function mostrar()
{
	//Genero el número RANDOM entre 1 y 10 
	var number;
	// sacado desde w3shcools sobre js Math.floor((Math.random() * RangoDeNumeros) + 1; (se suma uno por que el rango empieza en 0)
	number = Math.floor((Math.random() * 10) + 1);

	alert(number);

}//FIN DE LA FUNCIÓN